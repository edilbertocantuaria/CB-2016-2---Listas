#include <stdio.h>
#include <math.h>
int main (){
        double raio, altura, volume;
scanf ("%lf", & altura);
scanf ("%.1lf", & raio);
    volume = (3.1416*raio*raio*altura);
printf("%1lf\n", volume);
return 0;
}
